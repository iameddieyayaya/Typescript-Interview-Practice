/*
Problem: Quick Sort

Given an array of numbers, return a new array sorted in ascending order.
Do not mutate the input array.

Quick sort chooses a pivot, partitions the remaining values into smaller and
larger groups, then recursively sorts those groups.

Example:
[10, 7, 8, 9, 1, 5] -> [1, 5, 7, 8, 9, 10]
*/
export function quickSort(numbers: number[]): number[] {
  // TODO:
  // Choose a pivot.
  // Partition values into less-than and greater-than groups.
  // Recursively sort each side and combine the results.
  throw new Error("TODO: implement quickSort");
}

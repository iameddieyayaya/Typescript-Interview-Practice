/*
Problem: Bubble Sort

Given an array of numbers, return a new array sorted in ascending order.
Do not mutate the input array.

Bubble sort works by repeatedly walking the array, comparing adjacent values,
and swapping them when they are out of order.

Example:
[5, 1, 4, 2, 8] -> [1, 2, 4, 5, 8]
*/
export function bubbleSort(numbers: number[]): number[] {
  // TODO:
  // Make a copy so the input is not mutated.
  // Repeatedly swap adjacent values when left > right.
  // Stop when the array is sorted.
  throw new Error("TODO: implement bubbleSort");
}

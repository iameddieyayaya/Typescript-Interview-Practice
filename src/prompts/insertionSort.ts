/*
Problem: Insertion Sort

Given an array of numbers, return a new array sorted in ascending order.
Do not mutate the input array.

Insertion sort builds the sorted result from left to right by inserting each
new value into the correct position among the items already processed.

Example:
[5, 2, 4, 6, 1, 3] -> [1, 2, 3, 4, 5, 6]
*/
export function insertionSort(numbers: number[]): number[] {
  // TODO:
  // Make a copy so the input is not mutated.
  // Walk left to right and shift larger values to the right.
  // Insert the current value into its correct spot.
  throw new Error("TODO: implement insertionSort");
}

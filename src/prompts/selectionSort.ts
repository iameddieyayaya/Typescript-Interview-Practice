/*
Problem: Selection Sort

Given an array of numbers, return a new array sorted in ascending order.
Do not mutate the input array.

Selection sort works by finding the smallest remaining value and placing it
into the next sorted position.

Example:
[64, 25, 12, 22, 11] -> [11, 12, 22, 25, 64]
*/
export function selectionSort(numbers: number[]): number[] {
  // TODO:
  // Make a copy so the input is not mutated.
  // For each position, find the smallest remaining element.
  // Swap it into place.
  throw new Error("TODO: implement selectionSort");
}

/*
Problem: Merge Sort

Given an array of numbers, return a new array sorted in ascending order.
Do not mutate the input array.

Merge sort splits the array into halves, recursively sorts each half, then
merges the two sorted halves together.

Example:
[38, 27, 43, 3, 9, 82, 10] -> [3, 9, 10, 27, 38, 43, 82]
*/
export function mergeSort(numbers: number[]): number[] {
  // TODO:
  // Split the array until you reach arrays of length 0 or 1.
  // Recursively sort each half.
  // Merge the two sorted halves into one sorted array.
  throw new Error("TODO: implement mergeSort");
}

/*
Problem: Binary Search

Given a sorted array of numbers and a target number, return the index of the
target if found. Return -1 if the target is not present.

Binary search repeatedly checks the middle of the current search range and
discards the half that cannot contain the target.

Example:
binarySearch([1, 3, 5, 7, 9], 7) -> 3
binarySearch([1, 3, 5, 7, 9], 4) -> -1
*/
export function binarySearch(numbers: number[], target: number): number {
  // TODO:
  // Use two pointers for the current search range.
  // Check the middle value.
  // Narrow the range until you find the target or exhaust the array.
  throw new Error("TODO: implement binarySearch");
}
